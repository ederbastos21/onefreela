info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/order/createOrder
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
