info:
  name: valid (credentials)
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/users/login
  body:
    type: json
    data: |-
      {
        "email" : "ederbastos21@gmail.com", "password" : "Abc12345+"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
