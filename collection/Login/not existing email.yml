info:
  name: not existing email
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/users/login
  body:
    type: json
    data: |-
      {
        "email" : "emailnaoexistente@gmail.com", "password" : "Abc123!232"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
