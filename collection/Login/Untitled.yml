info:
  name: Untitled
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/login
  body:
    type: json
    data: |-
      {
        "email" : "exemploab@gmail.com", "password" : "Abc123!232"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
