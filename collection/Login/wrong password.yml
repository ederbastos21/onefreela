info:
  name: wrong password
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/users/login
  body:
    type: json
    data: |-
      {
        "email" : "exemploab@gmail.com", "password" : "Abc123!2321111"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
