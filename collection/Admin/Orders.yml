info:
  name: Orders
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/admin/orders
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
