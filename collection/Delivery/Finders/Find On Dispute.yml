info:
  name: Find On Dispute
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/delivery/findOnDispute
  headers:
    - name: Authorization
      value: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
