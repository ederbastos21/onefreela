info:
  name: Find Completed
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/delivery/findCompleted
  headers:
    - name: Authorization
      value: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
