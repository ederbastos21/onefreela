info:
  name: normal
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/delivery/makeDelivery
  headers:
    - name: Authorization
      value: ""
  body:
    type: multipart-form
    data:
      - name: message
        type: text
        value: envio de arquivo x
      - name: orderItemId
        type: text
        value: "1"
      - name: files
        type: file
        value:
          - /home/eder/Downloads/teste.txt
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
