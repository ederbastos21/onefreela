info:
  name: invalid min price
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8080/works/search?minPrice=-10
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
