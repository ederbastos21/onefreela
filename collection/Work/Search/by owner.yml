info:
  name: by owner
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/works/search?ownerId=
  params:
    - name: ownerId
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
