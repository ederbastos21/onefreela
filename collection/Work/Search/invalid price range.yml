info:
  name: invalid price range
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8080/works/search?minPrice=1000&maxPrice=100
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
