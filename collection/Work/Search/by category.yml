info:
  name: by category
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/works/search?category=
  params:
    - name: category
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
