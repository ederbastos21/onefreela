info:
  name: by price range
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/works/search?minPrice=100&maxPrice=1000
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
