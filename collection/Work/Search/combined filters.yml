info:
  name: combined filters
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/works/search?q=api&category=programacao&minPrice=100&maxPrice=1000&ownerId=1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
