info:
  name: without token
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:8080/works/deleteWork/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
