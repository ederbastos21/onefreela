info:
  name: without token
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8080/reports/admin/all
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
